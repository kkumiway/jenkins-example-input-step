pipeline {
  agent any
  stages {
    stage('input') {
      steps {
        input(message: 'Hello World!', ok: 'Submit')
        sh '''
          sh 'fish -c "hostname; sw_vers"'
        '''
      }
    }
  }
}
