pipeline {
  agent any
  stages {
    stage('input') {
      steps {
        input(message: 'Hello World!', ok: 'Submit')
        sh '''
          fish -c "hostname"
          fish -c "sw_vers"
        '''
      }
    }
  }
}
